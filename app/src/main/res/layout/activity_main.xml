<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Menu General"
        android:gravity="center" />
    <Button
        style="@style/btnMenu"

        android:text="@string/app_ajout"
        android:background="@drawable/shaped"

        android:id="@+id/btnAjout"/>
    <Button
        style="@style/btnMenu"

        android:text="@string/app_edite"
        android:background="@drawable/shaped"

        android:id="@+id/btnEdite"/>
    <Button
        style="@style/btnMenu"

        android:background="@drawable/shaped"
        android:text="@string/app_lister"
        android:id="@+id/btnLister"/>

</LinearLayout>